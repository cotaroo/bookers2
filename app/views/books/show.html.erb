 <div class="container">
   <div class="row">
     
   <%= render 'books/user_info', user:@user, book:@new_book %>
   
   
   <div class="col-md-8 offset-md-1">
     <h1>Books detail</h1>
     
     <table class="table">
       <tbody>
         <tr>
           <td><%= link_to "ここに画像",book_path(@book.user_id) %> </td>
           <td><%= link_to @book.title, book_path(@book.user_id) %></td>
           <td><%= @book.body %></td>
           <td></td>
         </tr>
       </tbody>
     </table>
     
   </div>
   
   </div>
</div>  